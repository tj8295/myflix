- @results.each do |category, videos|
  %article.video_category
    %header
      %h3= link_to "#{category.name}"
    .videos.row
      - videos.each do |video|
        .video.col-sm-2
          = link_to video, nil do
            = image_tag "/tmp/#{video.small_cover_url}"
